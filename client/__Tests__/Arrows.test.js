import React from 'react';
import { shallow, mount, render } from 'enzyme';
import Arrows from '../components/Arrows.jsx'

const wrapper = shallow(<Arrows />)

describe('Arrows render', ()=> {
  it('Should render nav arrows to the DOM', ()=> {
    
  })
})